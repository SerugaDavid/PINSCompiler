typ int:integer;
typ str:string;
typ log:logical;

fun main(arg:int):int = (
    {for i = 0, 10, 1 : (
        {a[i] = 3*i},
        print_int(i)
    )},
    #print_int_arr(a, 10),
    0
) { where
    var a:arr[10]int;
    var i:int
};

fun print_int_arr(array:arr[10]int, size:int):log = (
    {for i = 0, size, 1 : print_int(array[i])},
    true
) { where
    var i:int
}